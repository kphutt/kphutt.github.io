{{- /*
  Generated by Hugo (enableRobotsTXT in hugo.toml), not a static file, so the sitemap URL
  below comes from baseURL rather than being a second place the site's address is written
  down. That duplication is exactly what caused the http:// canonical bug.

  Everything here is allowed. Nothing on this site is private, and the default for an
  unlisted crawler is already "allowed" -- so the named blocks below change no behaviour
  today. They are here as an explicit, readable statement of intent, so that a future
  reader can tell "we allow AI crawlers" apart from "nobody ever thought about it".
*/ -}}
User-agent: *
Allow: /

{{ range slice
  "GPTBot"
  "OAI-SearchBot"
  "ChatGPT-User"
  "ClaudeBot"
  "Claude-User"
  "Claude-SearchBot"
  "PerplexityBot"
  "Perplexity-User"
  "Google-Extended"
  "Applebot-Extended"
  "CCBot"
  "Amazonbot"
  "meta-externalagent"
-}}
User-agent: {{ . }}
Allow: /

{{ end -}}
Sitemap: {{ "sitemap.xml" | absURL }}
