{{- /*
  Generated by Hugo (enableRobotsTXT in hugo.toml), not a static file, so the Sitemap URL
  below comes from baseURL rather than being a second place the site's address is written
  down. That duplication is what caused the http:// canonical bug.

  The policy: this site may be indexed, and may be read live to answer someone's question
  with a link back. It may not be used as training data.

  That split is the whole point of naming crawlers individually. Training and retrieval
  are separate bots now, so declining the first does not cost you the second. Blocking
  Google-Extended does NOT affect Googlebot or search ranking -- it is a control token for
  model training only, and the same is true of Applebot-Extended.

  robots.txt is a request, not an enforcement mechanism. Well-behaved crawlers honour it;
  nothing stops one that does not. The Content-Signal block below is what states the
  restriction as a reservation of rights rather than merely a preference.
*/ -}}
# As a condition of accessing this website, you agree to abide by the following
# content signals:

# (a)  If a Content-Signal = yes, you may collect content for the corresponding
#      use.
# (b)  If a Content-Signal = no, you may not collect content for the
#      corresponding use.
# (c)  If the website operator does not include a Content-Signal for a
#      corresponding use, the website operator neither grants nor restricts
#      permission via Content-Signal with respect to the corresponding use.

# The content signals and their meanings are:

# search:   building a search index and providing search results (e.g., returning
#           hyperlinks and short excerpts from your website's contents). Search does not
#           include providing AI-generated search summaries.
# ai-input: inputting content into one or more AI models (e.g., retrieval
#           augmented generation, grounding, or other real-time taking of content for
#           generative AI search answers).
# ai-train: training or fine-tuning AI models.

# ANY RESTRICTIONS EXPRESSED VIA CONTENT SIGNALS ARE EXPRESS RESERVATIONS OF
# RIGHTS UNDER ARTICLE 4 OF THE EUROPEAN UNION DIRECTIVE 2019/790 ON COPYRIGHT
# AND RELATED RIGHTS IN THE DIGITAL SINGLE MARKET.

User-agent: *
Content-Signal: search=yes, ai-input=yes, ai-train=no
Allow: /

# Training crawlers. A crawler obeys the most specific group that matches it, so these
# override the blanket Allow above.
{{ range slice
  "GPTBot"
  "ClaudeBot"
  "Google-Extended"
  "Applebot-Extended"
  "CCBot"
  "Amazonbot"
  "meta-externalagent"
  "Bytespider"
-}}
User-agent: {{ . }}
Disallow: /

{{ end -}}
# Search and answer crawlers: these cite the page and link back, so they stay welcome.
{{ range slice
  "OAI-SearchBot"
  "Claude-SearchBot"
  "PerplexityBot"
-}}
User-agent: {{ . }}
Allow: /

{{ end -}}
# User-triggered fetches: someone pasted a link into an assistant and asked about it.
# Blocking these would only inconvenience a reader who is already reading.
{{ range slice
  "ChatGPT-User"
  "Claude-User"
  "Perplexity-User"
-}}
User-agent: {{ . }}
Allow: /

{{ end -}}
Sitemap: {{ "sitemap.xml" | absURL }}
